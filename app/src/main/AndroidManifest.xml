<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >
    <uses-permission android:name="android.permission.INTERNET" />
    <application

        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/free_icon_open_book_3924850"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyBrain"
        tools:targetApi="31">
        <activity
            android:name=".Sys.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Sys.regClass" />
        <activity android:name=".ui.TaskManager" />
        <activity android:name=".Sys.FirstSee" />
        <activity android:name=".ui.dashboard.TheoryContentActivity"
            android:label="Теория">
            <!-- если нужна обработка deep link или intent-filter, здесь можно добавить -->
        </activity>
    </application>

</manifest>